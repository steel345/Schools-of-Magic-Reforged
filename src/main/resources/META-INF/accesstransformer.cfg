# Expose AbstractContainerScreen.renderSlot(GuiGraphics, Slot) so the herb pouch GUI
# can render its slot contents 1px smaller.
public net.minecraft.client.gui.screens.inventory.AbstractContainerScreen m_280092_(Lnet/minecraft/client/gui/GuiGraphics;Lnet/minecraft/world/inventory/Slot;)V # renderSlot

# The magic mirror's scrying view renders the world a second time from the remote
# camera. LevelRenderer keeps one ViewArea and one ChunkRenderDispatcher, both built
# around a single camera, so the second pass needs its own renderer or it evicts and
# rebuilds the chunks around the player every frame. Dropping final here lets us swap
# Minecraft.levelRenderer for the duration of that pass.
public-f net.minecraft.client.Minecraft f_91060_ # levelRenderer
